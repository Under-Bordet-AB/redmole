# Project build instructions

This project uses ESP-IDF.

The agent must not use shell aliases such as `idf`, because aliases may not be available in the agent terminal.

To build the project, always run:

```bash
./scripts/build.sh